//하단 추천 버튼 눌렀을때 화면으로 사용되는 파일

import 'package:flutter/material.dart';

class RecommendScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("RecommendScreen")),
    );
  }
}