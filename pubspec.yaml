name: market_kurly_ui
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ Google Fonts
  google_fonts: ^6.2.1

  # ✅ SVG 파일
  flutter_svg: ^2.0.10+1

  # ✅ 날짜 및 숫자 포맷팅
  intl: ^0.19.0

  # ✅ pull-to-refresh
  pull_to_refresh_flutter3: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true

  # ✅ 자산 폴더 등록
  assets:
    - assets/icons/
    - assets/images/
